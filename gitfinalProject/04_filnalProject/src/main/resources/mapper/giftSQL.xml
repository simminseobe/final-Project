<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper  namespace="gift">
	<insert id="insertCategory" parameterType="pc">
		insert into product_category values (pc_seq.nextval, #{pcCategory}, #{pcNo}, #{pcDepth})
	</insert>
	<select id="selectAllCategory" resultType="pc">
		select 
			pc_no pcNo,
			pc_category pcCategory,
			pc_step pcStep,
			pc_depth pcDepth
		from product_category
		where pc_step is not null
	</select>
	<select id="selectOneCategory" resultType="pc" parameterType="_int">
		select 
			pc_no pcNo,
			pc_category pcCategory,
			pc_step pcStep,
			pc_depth pcDepth
		from product_category
		where pc_no = #{_parameter}
	</select>
	<select id="selectStepCategoryList" resultType="pc" parameterType="_int">
		select 
			pc_no pcNo,
			pc_category pcCategory,
			pc_step pcStep,
			pc_depth pcDepth
		from product_category
		where pc_step = #{_parameter}
	</select>
</mapper>
